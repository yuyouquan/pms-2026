# 资源工作区后续交互回归

范围：基线 `4343c8c` 后的用户追加要求，包括常驻编辑、部分日期占比、自由版本号、缺模型阶段展示、年份摘要、绑定项目跳转、紧凑月表和固定列/输入框对齐。使用独立 feature 工作树，未修改或清理原始根目录的本地工作。

## 第一轮：领域及组件行为

- `npm run verify:budget-scheduling`：通过；包含部分日期、无有效间隔、缺失模型/模板阶段展示及严格排布校验。
- `npm run verify:resource-formal`：通过；包含自由版本名及重复/空白拒绝、保存与复制、年份统计、比例与月度平衡正式版本门禁、费率权限和持久化。
- `npm run verify:resource-inline`：通过；直接展示编辑器、初始不进入编辑会话、失焦保存、Escape恢复、错误保留、只读和外部更新。
- `npm run verify:resource-versions`：通过；版本生命周期、空白/复制、快照和重新加载。
- `npx tsc --noEmit`、`npm run build`、`git diff --check`：通过。构建仅有现存 Browserslist 数据过期提示。
- 版本自由文本、部分日期占比和常驻输入行为分别先复现原实现失败，再验证修复通过。

## 第二轮：本地生产构建浏览器交互

在 `http://127.0.0.1:3051/` 使用实际页面操作，不注入应用 store 或修改 localStorage。创建独立 QA 版本保留原版本数据。

- 技术跨年样例：全周期97.5人月/487.50万元；选择2026年为15.0/75.00、月均7.5；2027年82.5/412.50、月均8.3；峰值及阶段比例随年份变化，计划区间保持全项目。
- 新建 `V回归 A-9月/候选` 成功且刷新保留；同名创建被拒绝并保留弹窗。
- 部分日期：仅概念阶段已知9天时显示100%，其他0%；继续填写后概念/计划/开发验证为9/14/3天及34.62%/53.85%/11.54%，STR5仍为空。
- 非法倒序日期保留输入并显示校验，Escape恢复旧日期；锁定后全部编辑控件和模型排布行隐藏，解锁后恢复。页面无重复里程碑标题。
- 绑定预算链接位于创建时间之后，点击进入对应正式项目资源空间。
- 常驻字段直接填写、失焦保存；比例改为101%时禁止设置正式版本；修回100%后部门合计28.5改为30，阶段人月随比例更新且合计30。
- 非人力合计5100均摊17个月为每月300；首月改为301后合计5101。
- 月度产品部首月2.9改为4.4后，部门合计28.5变为30并显示已平衡，全版本129.6变为131.1；费用页相应首月22.00万元、部门150.00万元、总额655.50万元，费用页编辑器数量为0。
- 比例合计和操作表头计算样式均为sticky，right分别约64px和0；数值输入中心与外框中心偏移为0。月表表头12px、正文行约33px，阶段名/数值分两行，已分配差额单行。
- 浏览器曾记录扩展异步消息通道关闭错误；未观察到应用崩溃或资源保存异常，线上发布后另行检查应用和平台运行错误。

缺配置模型的结构回退由领域脚本和独立代码审查验证，未为浏览器测试修改现有用户配置。发布精确提交、远端引用和线上复验记录在本次发布交付文件中。
