# 人力资源管道合并验证

## 合并范围

- 基线：远端 `dev` 的 `403ecad672b81d6a0e26fa88e780ca6a6f824e92`。
- 完整合入 `origin/feature/hr-pipeline` 的 `893a6e51412f69b3ffc1eefb33bf057b1033eaf4` 及其历史提交。
- 58 个人力资源相关文件均已比对。包含整机产品、tOS、技术、能力建设项目的人力投入，以及配套配置、版本空间、月度投入和导出。
- 页面入口冲突保留双方功能：联合项目空间及现有项目访问边界继续保留，新增人力资源管道入口。
- 原分支中的资源总览、其他项目及非人力投入仍为占位页；本次不将这些入口声明为已实现。

## 发布前修复

1. 整机及技术项目的月度编辑弹窗依赖中重复引用投入列表，遗漏选中记录 ID。浏览器复现点击编辑后弹窗不出现，修复为随记录 ID 更新。
2. 月度投入保存仅关闭全局弹窗状态，未通知列表内的弹窗拥有者。浏览器复现保存后弹窗仍打开；四类项目保存成功后均通知拥有者关闭。
3. 数据规范检查识别固定演示占位值“当前用户”，同时保留对混入普通姓名的拒绝校验。
4. 清理源分支两处文件尾部多余空行，无功能变化。

除上述四个月度编辑组件修复外，其余人力资源文件与源分支内容一致（忽略文件尾空行）。

## 本地验证

- 合并前及最终合并内容均通过 `npx tsc --noEmit` 和 `npm run build`。
- `npm run verify:mock-data`、`node scripts/verify-session-boundaries.mjs`、`npm run verify:collapsible-sidebars` 通过。
- 四类项目各三个页面、新建弹窗、六类配置表、侧栏收起展开：23 项浏览器检查通过。
- 四类项目各连续操作两条月度记录：对应部门正确、合计不匹配时阻止保存、取消丢弃修改、有效保存关闭弹窗，8 组检查通过。演示分摊存在小数舍入差额时，先在表单内调整至预估合计再验证保存。
- 四类项目均成功创建并在刷新后保留；Excel 导出下载成功，文件可解析且包含 4 条项目记录。
- `PMS_BASE_URL=http://127.0.0.1:3028 node screenshots/verify-session-access-browser.mjs` 通过：非成员拒绝访问、普通成员权限、切换用户清理旧编辑状态。
- 浏览器控制台 0 错误；现有权限回归检查的运行时及 HTTP 错误列表为空。

手工浏览器检查使用独立会话；本地证据保存在 `output/playwright/hr-release/`。原始工作目录的未提交内容保留。
