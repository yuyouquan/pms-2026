# 路标项目编辑与基础信息待办验证

## 最终行为

- 预算项目不生成基础信息待办，覆盖整机、tOS、技术、能力建设以及已绑定/未绑定项目；预算编辑弹窗不再提示保存后移除待办。
- 路标项目空间复用原 tOS 路标 `PlannedProjectModal`，保留“项目分类与识别”“产品与版本”“时间与备注”及原必填规则。空间“编辑”和待办“去填写”使用同一个弹窗。
- 项目名从统一项目配置带入且只读；空间保存不修改名称、编码、绑定关系、创建信息、责任人及未出现的表单字段。保存仍经过统一项目 store 的权限、校验和历史记录。
- 空日期正确回显为空；STR5/上市日期必填且上市时间不能早于 STR5。日期的“预估”标记和备注可保存，未保存草稿取消时二次确认。
- 更换芯片编码同时更新空间展示的芯片型号、芯片平台；保留历史芯片时不改写关联历史值。
- Mock 包含资料完整/不完整的预算项目，以及仅缺两项日期的路标项目。已有浏览器持久化数据不被覆盖。

## 验证

- `npx tsc --noEmit`：通过。
- `npm run build`：通过（最终代码已重新构建）。
- 行为脚本通过：`verify-roadmap-space-editor.mjs`、`verify-project-info-todos.mjs`、`verify-fan-trial.mjs`、`verify-project-registry-final-fixes.mjs`、`verify-project-management-reaudit.mjs`、`verify-roadmap-registry.mjs`、`verify-enum-consumers.mjs`、`verify-project-info-modal-jira-save.mjs`。
- 独立本地 QA 页面（3047）实际操作：预算搜索 0 条；路标待办跳入原弹窗；空日期和反向日期阻止保存；填写日期、预估标记和备注后保存成功；重新打开回显一致；取消草稿需确认且不覆盖已保存备注；任务计数从 19 降为 18，刷新后搜索该路标待办仍为 0 条。
- 在 QA 枚举配置中新增芯片映射，再通过路标弹窗更换芯片，空间扩展信息显示相应编码、型号和平台；页面 error 日志为空。
- 3042 旧持久化记录也使用原弹窗。桌面 1280px / 窄屏 390px 检查通过，弹窗、表单行、底部操作区均无横向溢出。缺少枚举配置时保留原表单提示。

本地预览 3042、3043 已更新。测试写入仅发生在独立 QA 数据中。
