# 人力投入与人力模型调整验收

日期：2026-09-09。基线：`origin/dev` 的 `0284d3e`。实现分支：`codex/hr-investment-model-20260909`，独立工作区，保留原目录已有修改。

## 已实现范围

1. 整机、tOS、技术、能力建设的预估投入空间按“项目名称（技术为 TDT 项目名称）→预算类型→版本号”展示；版本、月度、详情与导出移除批次展示和编辑，旧存储字段兼容保留。
2. 年度预算最新版本使用手动里程碑；整机等级同样可手动修改，绑定和正式计划发布不会覆盖。非年度最新版本仍联动正式项目主市场／主类型最新已发布一级计划，历史版本保持快照。
3. 整机新建版本删除提示区和版本规则。复制模型时校验当前启用的等级与模型组合；非年度绑定版本优先按正式等级匹配模型，提交前再次验证。
4. tOS、技术、能力建设的新建版本复制同项目同预算最新版本的全部部门和阶段投入；不存在该预算时复制项目最近创建版本，编号仍从 V0.1 起。草稿修改不影响来源版本。
5. 三类版本弹窗及人力模型配置使用一级／二级部门联动下拉。候选来自现有配置及保存版本中真实存在的部门组合；切换一级部门清理不匹配二级部门，提交和导入均验证组合。
6. 人力模型新建、编辑桌面表单每行三字段，窄屏自动换行；启停需二次确认且同模型版本全部等级、部门一起更新。新增、移入及导入同版本记录继承该版本启停状态。
7. 新增按钮右侧提供“模型版本统计”，完整汇总每个模型版本、每个等级的六阶段及合计，包含禁用模型并标注状态，不受列表搜索条件影响。

## 验证证据

- `npm run verify:hr-investment`：102 条通过，含手动年度值绑定／同步／重载保留、非年度计划联动、历史快照、月度分配。
- `npm run verify:hr-version-refinement`：16 条通过，含同预算优先、跨预算最新创建时间、独立编号、历史保护、禁用／删除模型和有效组合校验。
- `npm run verify:hr-model`：26 条通过，含部门层级、整组启停、其他版本隔离、状态继承、分阶段统计与空版本兼容。
- `npx tsc --noEmit --incremental false` 通过；最终 `npm run build` 通过，完成全部 8 个静态页面构建。
- 四类实际界面流程通过：列顺序、批次移除、绑定年度预算编辑、历史只读、新版编号；三类部门表逐行逐阶段回填、联动选项、草稿隔离及跨预算 V0.1 创建。
- 1600／1280 两种宽度下四类项目列表、预估空间、月度表共 24 组列头／数据／汇总坐标检查通过。
- 模型界面 17 项交互、统计表 11 列坐标、430px 窄屏表单及底部按钮可达检查通过。
- 最终生产模式下再次完成四类版本完整流程，且禁用旧模型后新建成功使用有效等级 C／V2025.4，预估投入 52；运行时错误为 0。生产刷新及模型统计／新建检查捕获 18 个成功请求，无请求失败或控制台错误。
- 需求审查和质量复查通过；审查发现的禁用模型回填、非年度模型默认等级、统计表固定列遮挡均已修复。

浏览器脚本、日志和截图位于 `output/playwright/hr-refinement-20260909/`、`output/playwright/hr-model-refinement-20260909/`（仅本地证据，不提交生成文件）。

首次构建曾因临时开发预览实际复用 `.next`，覆盖生产清单而失败。关闭开发预览、保留冲突缓存至临时目录后重新构建成功；最终预览仅运行生产构建。

## 交付边界

当前应用仍为前端 mock 数据，数据保存于浏览器 localStorage。本轮交付本地 dev，不包含新增后端接口或数据库；未推送远端、未发布 Vercel。
