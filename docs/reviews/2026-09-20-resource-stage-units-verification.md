# 资源阶段、单位与紧凑月表验证

## 范围

整机开发/验证阶段拆分及旧数据迁移；正式项目里程碑分组；基础字段与预算排布同排；人力/非人力 tab 单位；比例与月度合计格式；月表移除整层阶段标题。

## 两轮验证

第一轮：领域/持久化和真实组件行为。

- `npm run verify:resource-inline`、`npm run verify:resource-formal`、`npm run verify:resource-versions` 通过。
- `npm run verify:budget-scheduling`、`npm run verify:level1-plan-governance` 通过。
- `node scripts/verify-non-labor-units.mjs` 通过：元/万元往返、分精度、模板单位防误用、输入回调、导入和导出。
- MR及技术计划回归由阶段迁移实施者执行通过；非人力原有导入/范围保留回归由单位实施者执行通过。
- 旧迁移自定义任务身份保留用例先失败后修复；新增空名称历史数组保守跳过保护。

第二轮：独立复核及本地生产构建页面。

- 独立复核发现旧模板迁移后英文 ID 显示及新增子项与 STR5 重号，均修复并再次复核通过。`verify-plan-task-identity.mjs` 覆盖身份去重与独立显示序号。
- 本地 `http://127.0.0.1:3051/`：原已持久化整机 V4 配置模板展示六阶段，原总周期100天和间隔保留，开发20天、验证20天；显示序号4/4.1/5/6正确。
- 示例整机-独立跨年预算：基础字段和日期按钮同排、分隔线可见；阶段显示开发/验证及上市&生命周期；表内单位省略；不完整日期仍计算阶段比例。
- 锁定 V0.2 显示相同阶段，排布控件隐藏。
- 月表仅有月份/年份一层标题；36.0/52.0（-16.0）实测颜色 rgb(207,19,34)、white-space nowrap。
- 示例tOS-独立跨年预算：保留开发验证阶段，后续名称为上市迭代&维护；比例100.00%/100%，表内无重复人月单位；月表平衡行显示43.2/43.2（0.0）。
- 复制弹窗打开后浏览器控制连接超时；本轮页面输入保存补验未在该连接完成，领域与真实组件保存回归已通过。线上检查另记于发布证据。

## 质量门禁

- `npx tsc --noEmit --pretty false`：exit 0。
- `npm run build`：exit 0（8个静态页面），仅既有 caniuse-lite 数据过期提示。
- `git diff --check`：通过。
- 原根工作区未清理、重置或覆盖；在 feature 工作树实施。
