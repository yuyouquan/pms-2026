# 人力资源管道人力投入调整验证

基线：远端 dev `62e910dca8623ef6cf83463e0e17847a390829e2`。

## 完成范围
- 四类项目列表移除取消暂停筛选；导出/新建放到筛选首行右侧；名称与正式编码相邻固定，绑定弹层不触发行跳转。
- 修正列表合计错位、末尾空白，版本表操作列裁切与固定列透底。
- 整机新建移除项目年份，按最新创建版本的概念启动/STR5生成立项/结项年份。
- 版本按项目和预算类型递增 V0.n；历史版本保护；绑定正式项目后采用主市场/主类型最新已发布一级计划，历史日期保留快照。
- 正式项目等级联动；最新机器版本仍可调整系数及模型，系数支持两位小数。
- 批次第1至20批替代锁定控制，历史版本也可调整，并在月度版本号之后显示。
- 修复能力建设月度部门丢失、预算汇总及分配舍入；缺少有效日期的投入保留待分配，不挤入其他月份。
- 模型变化后，最新机器版本、项目和月度总量同步；历史版本数据保留。

## 验证证据
- `npm run verify:hr-investment`：94项断言通过，覆盖编号、批次、历史保护、正式计划作用域/发布状态/阶段投影、年份、持久化、部门同步、模型同步、缺失和倒序日期及舍入。
- `npx tsc --noEmit`、`npm run build`：通过。
- 项目列表/月度列表：四类 × 1600/1280窗口宽度，共16组；另4组表头/数据/合计几何对齐检查通过。
- 月度编辑：四类各2个部门，共8组，选中部门、非法合计阻止、取消丢弃、保存关闭通过。
- 四类新建项目、刷新保留及Excel下载通过。
- 四类版本新增、历史批次20、最新批次3、月度同步、历史只读通过。
- 四类未绑定日期编辑、创建下一版本、实际正式项目绑定、历史日期保留及绑定后只读通过。
- 最终生产模式：四类三页、4类新建弹窗、6个配置入口和侧栏，共23组基础流程通过，无页面或控制台错误。
- 最终生产版本表：四类 × 1600/1280窗口，共8组，按钮边界及固定背景检查通过。
- 最终生产等级系数：新建1.25、行内修改1.35后，存储/列表/详情一致；历史版本保留1.25，无页面或控制台错误。
- 生产资源请求18项，失败0项。
- 浏览器截图和导出样例位于 `output/playwright/hr-investment-20260909/`，不纳入代码提交。

## 数据边界
应用仍使用现有浏览器本地模拟数据。旧演示IPM编码若在正式项目库中不存在，会保留快照并提示重新绑定；存在正式项目但缺少已发布主计划时，日期保持为空、对应投入待分配。

## 本地保护
原工作目录在 `codex/preserve-local-dev-20260820`，状态与完整差异均与执行前快照一致。修改在独立工作区完成。
