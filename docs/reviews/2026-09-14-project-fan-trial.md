# 整机项目粉丝试用配置验收

日期：2026-09-14。分支：`codex/feature-project-management-space`。

## 行为

- 配置中心 → 枚举值配置新增“粉丝试用国家”，复用枚举的搜索、新增、编辑、删除、权限和持久化；侧栏总数按注册表计算。
- 整机项目扩展信息新增必填“是否粉丝试用”，正式、预算、路标整机项目均适用，新建和旧数据缺省为“否”。
- 选择“是”后整行展开国家多选和国家/台数表格；至少一个国家，每国台数为正整数，不重复，显示合计。校验同时覆盖表单、折叠分组提交和项目 store。
- 选择“否”隐藏国家配置与空间明细，保留已填台数；重新开启继续使用。配置中心删除国家不篡改已保存项目，历史国家标为已停用，不能用于新选项。
- 项目空间展示国家台数及合计，修改历史展示具体国家与前后台数。缺少必要配置会被基础信息待办识别。
- 新增单国、多国 Mock 示例，保留其他默认“否”的场景。新示例只用于首次加载的 Mock 数据，不覆盖已有浏览器项目记录。
- 沿用统一表格字号、浅紫表头、32px 控件与整行扩展布局；修复窄屏单列网格被 JIRA 表格撑宽的问题。

## 验证

- `npx tsc --noEmit` 通过；生产构建通过。
- 11 项脚本通过：fan-trial、enum-config、enum-consumers、whole-machine-project-fields、project-info-matrix-refresh、project-info-followup-adjustments、project-info-modal-jira-save、project-info-todos、project-registry、project-registry-final-fixes、project-management-reaudit。
- 更新原字段/枚举数量契约，旧独立验证器使用真实依赖加载；修正旧 JIRA 验证器仅匹配 const 的过期断言，保留原行为校验。
- 浏览器 3046：多国明细及合计、空台数、小数校验与修正重试、清空全部国家、选择新国家逐国填写、关闭保存后重新开启、刷新持久化、新增“加纳”、删除已被使用的“肯尼亚”后历史配置保留并继续保存。
- 浏览器 3042：旧项目显示默认“否”；窄屏 390px 下编辑区宽度 285px，无横向溢出，台数及移除按钮完整可见。测试草稿取消，未修改已有项目数据。
- 浏览器运行时无 error。自动化辅助功能快照曾保留按钮旧的 loading 名称；通过真实 DOM 状态及点击复核，校验后按钮恢复且可重新保存。

最终构建实测：表头 14px，正文、台数输入和合计均为 12px；表头背景 `rgb(245, 243, 255)`。
