# 项目管理与项目空间资源：交付与验收记录

状态：功能、Mock 刷新与交互验收通过；业务代码已推送 feature，本记录随文档提交交付。

## 交付范围

按 [设计](../specs/2026-09-11-project-management-resource-design.md) 和 [实施计划](../plans/2026-09-11-project-management-resource.md) 实施。交付到 `codex/feature-project-management-space`；本次不包含主干合并或生产发布。当前应用仍为 Mock 与本地持久化，IPM 来源沿用现有外部项目池 Mock。

## 已确认规则与实施裁定

| 规则 | 依据与原因 | 调整成本 |
| --- | --- | --- |
| 单元格失焦后确认保存，取消恢复；名称链接进入空间，旁侧图标编辑 | 用户明确确认 | 局部交互调整 |
| 正式里程碑来自本项目已发布一级计划，只读；预算里程碑手工维护，绑定也不跟随 | 用户最终更正；历史快照沿用既有规则 | 日期来源与快照迁移需复核 |
| 保留跨项目人力汇总，项目空间共用实际版本，筛选与弹窗状态隔离 | 用户明确确认 | 调整投影视图，无须复制数据 |
| 路标预估投入本期为空，不允许建版本；资源看板均为空 | 用户明确确认 | 后续功能增量 |
| 非正式项目只能绑定同类型正式项目；每个正式项目每种来源属性最多一条；解绑保留来源数据 | 用户确认 | 绑定校验可逆调整 |
| 正式 IPM 项目标识唯一；不同正式来源可有相同来源编码，手工编码不得冲突 | 现有权威来源存在重复编码；不擅自改写源值 | 校验规则可逆调整 |
| 项目配置管理沿用管理组；普通空间编辑不能绕过配置修改名称、编码、绑定 | 保持配置职责及原项目编辑权限 | 权限和编辑范围可逆调整 |
| 项目资源复用本项目 basicInfo:查看/编辑；关联年度预算同时检查来源可见权限 | 既有系统没有独立资源授权项；避免从正式项目绕过来源权限 | 如拆分独立资源权限需迁移 |
| 模型编辑增加可配置 configCenter:hrModelEdit，从各角色既有 planEdit 初始化，后续独立配置 | 沿用配置中心可配置权限，不硬编码管理员，不耦合后续授权 | 默认授权可逆调整 |
| 全局角色与授权一并持久化，旧存储未包含时沿用默认值 | 确保自定义模型角色及独立授权在刷新后保留；此前这些全局设置仅在会话内保存 | 可逆存储迁移，不改变原默认授权 |
| 整机三项品牌信息仅在未绑定预算项目时必填；绑定项目缺少源值时提示补充但不阻断版本创建 | 用户仅规定未绑定时需填写；绑定字段仍随正式项目只读 | 局部校验与提示调整 |
| 新绑定与解绑以统一档案中的品牌信息为准；旧已解绑记录仅在人力数据中保留的来源信息需兼容保留 | 避免切换来源后错误恢复旧值，也不回退已有手工数据；按确定项目 ID 和已有来源快照判断 | 小范围来源标记迁移 |
| 旧人力身份不明或重复绑定保留为待核对只读记录；不按名称猜测 | 保护原版本、历史及手工月度投入 | 人工核对后可补确定归属 |
| 明确删除记录才触发关联资源移除；加载时暂时缺失不能当成删除 | 浏览器回归发现热更新期间的缺失处理风险 | 无额外数据迁移 |
| Mock 刷新不提升会清空存储的整套数据版本标记 | 保留原持久化数据及用户新建版本；新场景用新来源验收 | 后续需显式非破坏迁移 |
| 保留原主工作区未提交内容；开发在独立 worktree | 原工作区存在其他工作 | 无数据变动 |
| 最后截图发现的悬浮透字仅局部修正配置表固定列背景 | 已授权的交互验收发现，限定 11 行 CSS 后重建并截图复测，不扩大功能或改全局主题 | 局部样式可逆调整 |

## 分段验证证据

| 范围 | 结果 |
| --- | --- |
| 统一档案、创建、绑定、历史和权限 | 14 组行为检查通过；独立审查通过 |
| Header、配置/视图、失焦确认与返回状态 | 导航及关联回归通过；浏览器验证创建、取消/保存、重复编码、历史、权限与筛选；独立审查通过 |
| 路标迁移和空间补录 | 原 142 项及 10 组集成检查通过；浏览器验证不完整行、日期/备注保存、责任人保持、tOS 单角色编辑保持其他角色、删除后刷新不复活；独立审查通过 |
| 共享人力版本与持久化 | 11 组归属检查、102 项投入、339 项审计、16 项版本检查通过；另 7 组修复回归通过；独立复审通过 |
| 旧人力数据迁移 | 实际旧快照的 79 个版本和 201 行月度记录 ID、投入值、手工标记均保留；重复迁移无变化 |
| 跨入口浏览器回归 | 新建年度预算、手工 STR5、完整刷新后，汇总 16→17 条，单项目仅 1 条；返回空间值一致，关联正式空间只读；本轮新日志无 error/warn |
| 完整表单与详情 | 9 组行为检查通过；四类实际创建、日期和投入详情回读通过；正式只读与来源权限通过；独立复审通过 |
| 模型入口、刷新 Mock | 模型授权与持久化及浏览器操作通过；原模型入口移除、其他配置保留；Mock 已刷新，12 组场景验证与独立审查通过 |
| 最终构建、全功能浏览器验收、远端分支一致性 | be2321f 通过 19 项业务命令；最终修复 0de739f 通过 6 组新增回归及相关套件、12 组 Mock 场景、类型检查、生产构建和浏览器复测；独立复审通过 |

旧数据迁移核对：整机 16 个版本/27 行月度，tOS 22/60，技术 22/60，能力 19/54。归属待核对仅为旧数据兼容，不能代替正常新功能示例。

## 已完成的实际交互核对

在冻结对应功能代码后，通过浏览器实际点击、填写、保存和刷新核对；新版 Mock 及最后修复均已在生产构建中完成验收。

- 配置：最小建档覆盖三种属性、四类项目；正式项目名称选择自动映射类型。非正式名称图标编辑与名称进入空间互相独立；编码失焦取消恢复、确认保存、重复编码拒绝且保留编辑；历史只记录成功操作。返回时保留分页及项目视图筛选。
- 路标：资料不完整时保留表格行，补充 STR5 与备注立即反映；补录不清空责任人；tOS 只改 SE 不影响版本经理或 SQA。删除自建验收记录后完整刷新，没有重新出现。
- 绑定：预算项目只能选择同类型且未被预算占用的正式项目；取消解绑保持关系，确认后来源年度预算与手工日期保留，正式空间关联消失。预算责任人为演示用户09时，演示用户02不能进入来源空间，也不能在其有权访问的正式空间看到来源年度预算。
- 四类版本：整机年度预算模型系数 1.25，预估合计 125，日期 2027-01-10/2027-12-01；tOS 六阶段输入 1–6，合计 21；技术五阶段输入 1–5，合计 15；能力部门投入 12.5，开始/结束 2027-04-01/2027-08-31。创建后详情读回与输入一致。
- 正式日期：整机概算合计 100，概念启动/STR5 使用本项目一级计划的 2026-01-15/2026-05-15；新增字段禁用、最新表格日期不出现编辑器。关联年度预算保持自己的 2027 年日期。缺少 IPM 编码及一级计划的正式能力项目仍可创建概算，日期留空且只读。
- 范围隔离：能力正式空间打开关联年度预算详情后，再开新版本，仍定位本正式项目；成功创建自身概算 13，来源年度预算 12.5 不变。年度预算在项目空间创建后完整刷新，在跨项目汇总与自身空间都能读回相同版本和日期，范围筛选互不影响。
- 模型：新入口复用原 8 条配置；新增验收模型、修改生命周期、完整刷新保留，再确认删除恢复 8 条。演示用户02只能查看，新增与导入禁用，行内编辑操作隐藏。旧人力资源管道中的模型入口已移除，其余五项配置保留。

## 新 Mock 与生产构建浏览器验收

新 Mock 使用稳定项目 ID 关联。全新环境初始化后共 53 个项目；四类项目各有正式版本、关联年度预算两版历史、独立跨年预算，并包含来源资料待补充和未填日期路标。整机来源年度预算由演示用户09负责，便于验证来源权限。正式日期取自本项目已发布一级计划，预算日期独立使用 2027 年。旧存储数据不因刷新 Mock 被清空。

在 `be2321f` 的生产构建、全新来源 `http://127.0.0.1:3042/` 中完成：

- 绑定但来源品牌信息为空的预算可建年度版本；三项只读显示“—”并有非阻断提示。未绑定空资料先拒绝创建，补齐品牌/产品线/市场名后成功。
- 正式整机显示自身 3 条及来源年度预算 2 条历史；演示用户02只看到有权限的自身 3 条。正式新增里程碑输入禁用。
- 实际修改并发布 OP 一级计划 V4，概念启动改为 2026-02-27；最新概算与项目预算自动更新，旧概算 V0.1 仍为 2026-01-15，来源年度预算的 2027 年日期不变。
- 来源空间把年度预算 STR5 改为 2027-09-15，汇总读回相同值；汇总再改为 2027-09-16，自身及关联正式空间都读回。月度原手工 1 月 0/2 月 8.8 未被覆盖；再手工改为 1/7.8，合计仍为 52。完整刷新后三类数据均保留。
- 创建两条验收年度版本后，跨项目历史恰为 9 条（原 7 + 新增 2）；年度预算合计 425、概算 100、预算 100，关联展示未重复计数。
- 解绑后正式空间移除两条关联年度历史，来源空间仍保留两版及手工日期。删除本轮新建的最后一条投入版本后，其项目档案仍存在。
- 配置编码失焦取消恢复原值，确认才保存，历史只记录成功的一条修改。新建责任人回归项目仅填四个字段，创建人和时间正确。
- tOS、技术、能力预算空间各有两版手工日期历史；四类资源看板均为空。路标估算为空且没有建版入口；未补日期路标仍在表格第 3 页，日期均为空；原完整路标样例仍保留。
- 项目视图切换全部项目后，搜索预算名为 0 条，搜索正式 DEMO017 得到对应项目。

生产构建成功，类型检查无错误，19 项业务验证命令全部通过。构建仅提示既有 Browserslist 数据陈旧，未变更依赖。生产浏览器未记录运行错误；一级计划发布触发现有 Feishu stub 的“SPM 未映射、跳过”警告，未发送外部通知。最终综合审查发现的责任人派生权限、绑定预算普通基础信息来源保护、长绑定名称控件遮挡，以及共享月度提示四处问题，已在同一轮修复并完成复审与实际交互回归。

## 最后修复与生产浏览器复测

最终业务提交 `0de739f`（`fix: close project registry responsibility and metadata gaps`）完成：

- 6 组新增行为回归全部通过；统一档案 14 组、HR 归属 12 组、版本表单 9 组、资源回归 7 组及角色同步通过。另在此提交重新运行 12 组新 Mock 场景、`npx tsc --noEmit`、`npm run build`，均退出 0。
- 独立复审的需求符合性与代码质量均为 Approved；原 3 项 Important、1 项 Minor 全部解决，没有新增问题。
- 1278px 默认宽度，横向滚动至绑定列可见后，长名称在 221.67px 列内缩略，编辑按钮宽 32px、右边缘 1084.95px，操作列从 1094.95px 开始。已绑定长名称后再次点击铅笔，正确打开单元格选择器，没有误开历史或删除。未滚至可见区域时固定列会覆盖离屏部分，因此验收采用实际可见控件。
- 绑定预算基础信息与编辑弹窗同时展示来源“示例品牌A / 示例系列A / 示例系列A 50 Pro”，三个输入均禁用。仍可保存手工 STR5=2028-06-15 和备注；完整刷新后回读一致。资源年度预算 STR5 仍为 2027-09-16，旧版仍为 2027-08-01，不受基础信息补录覆盖。
- 再次确认解绑后，基础信息保留上述三个最后有效值，资源保留两版年度历史和手工日期。之后恢复示例绑定。月度投入第一部门仍为 1 月 1.0、2 月 7.8、预估合计 52.0。
- 演示用户02自行将自建预算项目“交互验收-责任人交接”的 SPM 改为09，保存成功后02立即显示无权查看；09可以编辑并新建资源版本。完整刷新后再次切换02与09，旧用户仍被拒绝、新用户仍可操作；最后恢复管理员。
- 共享月度提示已在实际页面显示：正式项目完善自身已发布一级计划，预算项目（含绑定）手工补充最新版本里程碑，能力建设补充开始/结束时间。浏览器无新增 error/warn；仅保留前述旧 Feishu stub 警告。

旧解绑资料采用明确项目 ID 与旧来源快照逐字段兼容：已显式修改的档案字段优先；未改项才采用旧人力记录中保留的最后值。归一后记录来源标记，重复刷新不再采用旧值。新绑定、解绑及删除快照不会重新触发旧兼容逻辑；A→B→A、先改一项再加载 HR、刷新幂等均有行为回归覆盖，内部标记不显示在用户历史中。

最后截图又发现固定名称列在悬浮时采用全局半透明背景，横向滚动会透出下方类型文字。追加局部 CSS 提交 `3bd61e5`，仅为配置表固定列设置不透明普通与悬浮底色。controller 检查 11 行差异后重新生产构建（退出 0），并在 1278px 横向滚动、悬浮截图中确认文字不再叠显，铅笔仍正常打开编辑器、未出现其他弹窗。该补漏没有改变业务逻辑；此前独立复审与业务回归结论保持适用。

## 最终交付

- 分支：`codex/feature-project-management-space`，基于 `dev` / `origin/dev` 的 `23b8d3c5c704484740cf8c40c01d44b89e59bb5e`；业务提交推送后已用 `git ls-remote` 核对远端 HEAD 为 `3bd61e59cab4770d0933033aa01658e360253677`。文档提交后再核对最终 HEAD。
- 新 Mock 提交：`be2321f`；最终业务修复：`0de739f`；最终样式修正及构建：`3bd61e5`；本验收记录由随后的文档提交收口。
- 本地生产预览：`http://127.0.0.1:3042/`。新环境初始化显示 53 条项目；验收来源另保留本轮实际创建和修改的测试数据，不影响仓库中的确定性 Mock。
- 原主工作区的未提交文件保持原样。本次仅交付 feature，不合并主干或发布生产。


推送命令退出 0，远端分支及本地跟踪分支一致。Git 曾输出本机凭据缓存写入提示 `failed to store: -25293`，但远端更新成功且已独立读取远端提交确认；没有因此重复业务写入。
