# 项目资源看板精简与阶段、累计投入

按本轮截图及确认口径更新：

- 年度预算、项目概算、项目预算仅纳入唯一正式版本，版本号在标题右侧；删除版本下拉、锁定状态与全部卡片底部说明/链接。缺失或冲突时不回退草稿。
- 四类趋势保留人月/费用及月度/周度，增加阶段。预算使用各自正式版本的里程碑及保存的排布模板；核算使用正式项目已发布里程碑。模板阶段沿用资源里程碑标题，整机含概念、计划、开发、验证、上市&生命周期。相邻边界不重复；缺失/倒置/重叠无法归类部分列未归属阶段，保持总量。计划的日粒度按每月周一至周五折算，核算按记录日期。
- 工时投入明细保留。删除实际非人力费用展开区与更多分析区；费用卡片/趋势仍包含非人力费用。
- 新增“累至今日的人力投入”，按一级/二级部门汇总人数、人天、人月与占比。仅随部门筛选变化，忽略顶部日期/年度，项目开始至本地今天（含当天），排除未来 Mock 记录；未提供项目开始时间时使用实际来源覆盖开始。人月逐条按人天/来源当月工作日计算。
- 导出六张表：四类投入、偏差及执行率、当前趋势、工时投入明细、累至今日的人力投入、口径与检查。累计表日期与上方分析日期分别注明。执行导出时重查权限、正式版本身份与最新投入数据。

验证：阶段边界/无日期/异常区间/各类型模板；月周阶段与卡片总数对齐；累计日期独立与未来数据排除；权限失效及正式版本变更时导出拒绝；Excel 回读；TypeScript 与生产构建；浏览器普通及空项目、部门/日期/周期切换、线上运行错误。

## 本地验收记录

- `verify:resource-dashboard-business`：通过，含阶段边界/无日期/重叠、月周阶段守恒、来源工作日、截止今日及部门过滤、独立日期口径、正式版本冲突、六表 Excel 回读。
- `verify:resource-dashboard`、`verify:resource-monthly-cost`、`verify:resource-permissions`：通过；正式版本撤回后旧导出回调会拒绝执行。
- `npx tsc --noEmit`、`npm run build`：通过。
- 生产构建端口 3054，DEMO017 → 资源 → 项目资源看板：六张卡片同一行，只有正式版本号，无下拉/锁定/卡片底部说明；阶段与月周可切换，人力与费用切换正常。
- 日期改为 2027-01-01～2027-01-15，累计区仍为截至 2026-09-22 的 15.945 人月；筛选研发中心为 12.410、软件部为 7.070，重置恢复 15.945。上方工时明细随日期变化。
- DEMOP21 无正式预算及核算来源：卡片显示空缺，阶段趋势与累计区显示空态，无草稿回退。
- 两条浏览器流程均无 error/warn 运行日志。
