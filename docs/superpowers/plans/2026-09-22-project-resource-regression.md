# 项目与资源全面回归验收（2026-09-22）

## 范围与基线

基于 dev `0281a58`，在隔离工作区完成配置中心人力资源、项目配置、项目视图数据同步、项目空间资源、工作台基础信息、tOS 路标信息补充的回归。原始工作目录中的未提交修改保持原样。

## 本次修复

1. 新建项目与后续编辑共用人员目录，覆盖普通基础信息、技术项目独立团队表单、权限角色和用户切换；具名责任人不会在后续下拉框中消失。
2. 路标编辑表单在 resetFields 后保留本次初始化值，修复弹窗打开后已有字段被清空、只读项目名为空而无法保存的问题。关闭和切换项目仍显式重置表单。
3. 权限持久化适配器遵守跨标签页 hydration 的禁止回写约束，消除新旧页面互相触发 storage 事件的循环。
4. 修正三个整机 Mock 种子项目中无法登录的 SPM 演示身份，使用已有可选用户；不重置用户现有浏览器数据。
5. 新建正式项目时按项目类型规范化来源状态，和持久化恢复使用相同口径，修复技术项目刷新前“待立项”、刷新后“进行中”的变化。

## 回归检查

- 首轮执行 97 个现有校验脚本，81 个通过、16 个失败。逐项区分真实缺陷与旧需求断言，修复缓存循环和 Mock 身份，并更新已被后续需求替代的断言。
- 第二轮完整执行 98 个脚本（新增实际人员组件回归），97 个通过；剩余一个项目导航脚本仍断言旧人员常量，更新为共享目录后单独复测通过。
- 浏览器发现技术独立表单和新建状态问题后，增加先失败后通过的行为回归，复测人员控件、项目登记持久化和导航；额外执行技术项目与 tOS 状态两项检查通过。最终覆盖 100 个脚本，所有检查均已通过。
- `npx tsc --noEmit`、`npm run build`、`git diff --check` 通过。构建只有现有 Browserslist 数据更新时间提示。
- 提供可重复入口：`npm run verify:project-resource-regression`，依次调度这 100 个脚本，三路并发，每项独立日志和退出状态写入系统临时目录。浏览器验收单独执行，不由脚本启动浏览器。

旧断言调整保持当前确认规则：未锁定历史版本可编辑；正式空间成员可读关联年度预算但不能编辑；上层投入变化重算月度，月度编辑不反改上层；既有版本保留所选模型快照；汇总使用正式版本；菜单包含费率；表格使用当前共享主题。锁定、越权和只读条件仍保留反向校验。

## 两轮浏览器验收证据

第一轮使用开发环境，第二轮使用同一代码的生产构建，均通过真实界面操作。

| 范围 | 实际操作和结果 |
| --- | --- |
| 配置中心人力资源 | 费率 5 改为 6.25，10 人月费用示例变为 62.5 万元；资源 102 人月费用变为 637.50 万元。模型、等级过滤、阶段拆分和序列化由专项校验覆盖。 |
| 项目配置 | 创建预算项目和 IPM 正式技术项目 DEMO-TECH-V3；模拟通知展示所选负责人、内容和时间，没有真实飞书发送。 |
| 项目视图 | 新建正式技术项目立即进入“全部项目”；非责任操作者的“仅看我的”不包含该项目。责任人账号只看到所属项目。 |
| 工作台基础信息 | 以邓伟俊打开 DEMO-TECH-V3 待办，填写领域、项目价值、年份，选陈佩玲为技术项目经理并保存；待办从 1 变为 0，项目视图同步显示团队和领域。 |
| 路标补全 | 示例整机-路标待补日期从工作台填写 STR5=2027-03-01、上市=2027-04-01，待办移除；tOS17.2 路标同步。随后添加备注，刷新并从路标重新编辑，项目名、日期、备注均保留。 |
| 资源生命周期 | 从 V0.2 复制创建 V回归-0922；比例 14.04% 改为 10% 后，月度分配随之减少，设正式版本被拒绝；恢复 100% 后再改月度 3.2→4.2，部门预估仍为 23.5，显示 24.5/23.5（+1.0）并拒绝正式版本；恢复后设正式成功。 |
| 资源视图 | 年份切换后显示 2026 年 16.1 人月/100.63 万元；费用表只读；锁定后无数字输入框和模型排布按钮；日志包含操作者、时间、比例和月度前后值、正式与锁定动作。 |
| 非人力投入 | 元单位和两位小数正确，输入 123.45 后月度与合计更新；投入时间范围位于投入 tabs 右侧。 |
| 看板 | 新建无版本项目明确显示未设置正式版本、无分析来源，不虚构金额；正式/手动版本分析计算由专项行为校验覆盖。 |
| 运行情况 | 生产构建浏览器完整流程未捕获 console error。新旧页面缓存同步、资源权限默认值和越权写入由专项回归通过。 |

## 边界

这是前端 Mock 原型验收。飞书通知仅为已确认的模拟流程；未接入真实飞书发送或后端数据库。Mock 身份修复适用于种子数据，不清空已有用户数据。
