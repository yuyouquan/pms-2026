# 月度资源费用包含非人力投入

费用口径：人力投入（人月）× 配置中心费率（万元/人月）+ 当前版本有效时间范围内的非人力投入（元）÷ 10,000。

- 全周期/年度总投入卡片、年份标签、费用柱图、费用表月度/年度/全周期及已分配合计采用同一口径。
- 费用表增加一行“非人力投入”汇总，避免按部门匹配遗漏费用或重复计算。
- 费用视图保持只读；人月、阶段分布及人力分配规则保持原口径。
- 保留至元的分位精度后再格式化；缩短日期范围时不统计隐藏费用，亦不删除原数据。

## 验证

- 新增 `verify:resource-monthly-cost`，覆盖金额换算、跨年、版本/归档隔离、仅有非人力费用的月份、重复部门、隐藏月份、费率变化、分位精度和实际组件切换。
- 用修改前的组件运行同一用例，复现总投入错误显示 500.00；修复后显示 510.00。
- 月度展示、资源看板、非人力投入、元/万元及费用范围回归脚本均通过。
- `npx tsc --noEmit`、`npm run build` 通过。
- 本地生产构建浏览器：整机正式项目 DEMO017-DEMOCHIP001_DEMOBOARD016 → 资源 → 项目概算 V0.2，在 2026-01 输入 100,000 元，卡片及表格合计从 500.00 变为 510.00 万元，1 月费用由 27.00 变为 37.00；人力仍为 100.0 人月。
- 切换 V0.1 显示 500.00，回到 V0.2 显示 510.00；费用视图无可编辑输入框。
- 关联年度预算：9,100 元费用计入 2027 年及全周期，2028 年仍为 33.00 万元；关联预算只读保留。
- 浏览器费用表布局已检查，控制台无 error/warn。
