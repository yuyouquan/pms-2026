# PMS 现有功能回归（2026-09-24）

基线：远端 dev `de2fb752afa9d5df43f708662d0cbf5cf3c15adf`。
工作分支：`codex/feature-pms-regression-20260924`。使用隔离工作区，未修改原工作区未提交内容。

## 修复

- 正式项目编辑时，旧数据 `normal` 被误识别为已停用枚举并显示 `normal（已停用）`。编辑表单与草稿恢复现在将 `normal`、`warning`／`attention`、`risk` 对应为“正常”“关注”“风险”。自定义历史状态保留；配置中心已停用的状态仍不可选。
- 新建表单的默认健康状态统一为“正常”，同时修正空草稿判断，避免仅默认值产生草稿。
- 修复 7 个过期检查脚本：配置导航迁移后的源码位置、两个市场规则检查的 TypeScript 依赖加载、系统同步操作人识别、资源“总览”名称、资源自动保存导航上下文，以及 tOS 跟随类型维护提示。保留相关业务断言，并新增真正未保存资源草稿仍需确认的检查。

## 自动检查

首轮执行 159 个独立 `verify-*` 脚本，152 个通过、7 个上述旧检查失败。修正后逐一复测，并新增健康状态枚举回归脚本，最终 **160/160 通过**。

覆盖项目创建与草稿、项目注册及跨模块同步、角色权限、枚举与人力模型、资源版本生命周期、预算与非人力费用、累计预估和执行率、月度分配、资源持久化、市场／tOS 计划、路标、工作台待办、转维流程。

- `npx tsc --noEmit`：通过。
- `npm run build`：通过。
- `git diff --check`：通过。
- 聚合资源回归入口未重复执行；直接运行了其独立检查。依赖独立 Puppeteer 的旧浏览器脚本未执行，实际交互使用浏览器工具完成。

## 浏览器验证

使用本地生产构建，测试数据仅保存在本地 Mock 环境。

| 功能 | 验证结果 |
| --- | --- |
| 项目配置 | 必填校验正常；新建预算项目后可筛选查询。测试项目已删除。 |
| 责任人同步 | 新建 tOS 项目所选责任人同时进入“版本项目经理”和“系统管理员”。 |
| 创建通知 | 模拟回执包含标题、正文和具体项目链接；访问链接进入该项目空间并自动打开编辑弹窗。未发送真实飞书消息。 |
| 健康状态 | 修复后的正式 tOS 项目编辑框显示“正常”，不再显示英文已停用值。 |
| 资源总览 | 部门明细默认选中，工时明细可切换；周度／阶段与费用维度切换正常。 |
| 资源费用 | 非人力投入增加 1,000 元时，总费用由 469.11 变为 469.21 万元；恢复总额后回到 469.11 万元。 |
| 资源自动保存 | 部门投入 16.5 改为 17.5 后切换页面并返回仍保存；离开不误弹确认。 |
| 版本管理 | 锁定后输入控件不再可编辑；解锁恢复；操作日志记录修改前后及锁定状态。 |
| 配置中心 | 人力模型与非人力科目正常加载；费率 5 改为 5.25 后，10 人月示例变为 52.5 万元；已恢复 5。 |
| 工作台／路标 | 待办筛选后“去填写”打开对应路标项目编辑框；表单与版本演进视图正常切换。 |
| 项目组合 | 1+N 计划列表正常显示，使用分段切换样式及独立内容边界。 |
| 权限 | 切换到未配置 tOS 项目角色的演示用户08，显示无访问权限且不再展示项目内容；已恢复管理员。 |
| 运行错误 | 本轮交互未发现浏览器 error 日志。 |

## 范围说明

当前系统仍使用 Mock 数据，IPM 数据接入和真实飞书发送不在本轮验证范围。模拟身份刷新会恢复默认演示用户；权限交互通过页面内身份切换验证，通知链接解析和授权边界另有自动检查覆盖。
